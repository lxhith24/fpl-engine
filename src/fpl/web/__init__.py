"""Local dashboard + chat panel over the current-gameweek picture.

`python -m fpl web` starts a FastAPI app on localhost that renders the optimal
XI, the manager's squad and its gap, differentials and matchup boosts, plus a
read-only Claude chat panel grounded in exactly that data.
"""
